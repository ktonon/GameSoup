#!/usr/bin/env python

from distutils.core import setup
from setuptools import find_packages

setup(
    name='GameSoup',
    version='0.1',
    description='A Two-Stage Game Develompent Environment.',
    author='Kevin Tonon',
    author_email='ktonon@cs.toronto.edu',
    packages=find_packages(),
    )
